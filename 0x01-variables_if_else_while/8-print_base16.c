#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: always returns 0
 *
 */

int main(void)
{
char first = '0';
char last = '9';
char First = 'a';
char Last = 'f';

while (first <= last)

putchar(first);
first++;

}
while (First <= Last)

{
putchar(First);
First++;

}
putchar('\n');
return (0);
}
