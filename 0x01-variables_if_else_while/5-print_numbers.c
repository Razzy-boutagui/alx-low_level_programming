#include<stdio.h>
/**
 * main - Entry point
 *
 * Return: always return 0
 *
 */
int main(void)
{
	int Number = 0;
	while (Number < 10)
	{
		printf("%d",Number);
		Number++;.
	}
	printf("\n");
	return (0);
}
